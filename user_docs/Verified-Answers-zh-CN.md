# 已验证答案

[English](Verified-Answers.md) | 简体中文

## 1-1 A到B (c1_1_atob)
**输入：** 一个由a,b和c组成的字符串。
**输出：** 将每个a替换为b。
**限制条件：** 1 &lt;= 输入长度 &lt;= 7
```
a=b
```

## 1-2 大写 (c1_2_uppercase)
**输入：** 一个由a, b和c组成的字符串。
**输出：** 将每个字母替换为相应的大写字母。
**限制条件：** 1 &lt;= 输入长度 &lt;= 7
```
a=A
b=B
c=C
```

## 1-3 去重 (c1_3_singleton)
**输入：** 一个由a, b和c组成的字符串。
**输出：** 将连续的相同字母替换为单个字母。
**限制条件：** 1 &lt;= 输入长度 &lt;= 7
```
aa=a
bb=b
cc=c
```

## 1-4 去重 2 (c1_4_singleton2)
**输入：** 一个由a, b和c组成的字符串。
**输出：** 删除连续的a。
**限制条件：** 1 &lt;= 输入长度 &lt;= 7
```
aaa=aa
aa=
```

## 1-5 排序 (c1_5_sort)
**输入：** 一个由a, b和c组成的字符串。
**输出：** 将输入按照字典序排序。
**限制条件：** 1 &lt;= 输入长度 &lt;= 7
```
ba=ab
ca=ac
cb=bc
```

## 1-6 比较 (c1_6_compare)
**输入：** 一个由a和b组成的字符串。
**输出：** 出现次数最多的字母。
**限制条件：** 1 &lt;= 输入长度 &lt;= 11
a和b的数量不同。
```
ab=
ba=
aa=a
bb=b
```

## 2-1 你好，世界 (c2_1_hello)
**输入：** 一个由a, b和c组成的字符串。
**输出：** helloworld
**限制条件：** 1 &lt;= 输入长度 &lt;= 7
```
=(return)helloworld
```

## 2-2 AAA (c2_2_aaa)
**输入：** 一个由a, b和c组成的字符串。
**输出：** 如果输入包括至少三个a，返回true。
否则，返回false。
**限制条件：** 1 &lt;= 输入长度 &lt;= 7
```
b=
c=
aaa=(return)true
=(return)false
```

## 2-3 恰好三个 (c2_3_exactly)
**输入：** 一个由a, b和c组成的字符串。
**输出：** 如果输入包括恰好三个字母，返回true。
否则，返回false。
**限制条件：** 1 &lt;= 输入长度 &lt;= 7
```
b=a
c=a
aaaa=(return)false
aaa=(return)true
=(return)false
```

## 2-4 余数 (c2_4_remainder)
**输入：** 一个由a, b和c组成的字符串。
**输出：** 将输入长度除以3。输出余数。
**限制条件：** 1 &lt;= 输入长度 &lt;= 7
```
b=a
c=a
aaaa=a
aaa=0
aa=2
a=1
```

## 2-5 奇数 (c2_5_odd)
**输入：** 一个由a, b和c组成的字符串。
**输出：** 如果输入中每个字母出现次数均为奇数，或者为0，返回true。
否则，返回false。
**限制条件：** 1 &lt;= 输入长度 &lt;= 7
```
ba=ab
ca=ac
cb=bc
aaa=a
bbb=b
ccc=c
aa=(return)false
bb=(return)false
cc=(return)false
=(return)true
```

## 2-6 独一无二 (c2_6_only)
**输入：** 一个由a, b和c组成的字符串。
**输出：** 如果输入中恰好有一个字母与其相邻字母不同，返回true。
否则，返回false。
例如在"babbcc"中，第一个a和第一个b与相邻字母不同，因此返回false。
**限制条件：** 1 &lt;= 输入长度 &lt;= 7
```
aaa=aa
aa=|
a=@
bbb=bb
bb=|
b=@
ccc=cc
cc=|
c=@
|=
@@=(return)false
@=(return)true
=(return)false
```

## 2-7 上升 (c2_7_ascend)
**输入：** 一个由a, b和c组成的字符串。
**输出：** 如果输入中c比b多，并且b比a多，返回true。
否则，返回false。
**限制条件：** 1 &lt;= 输入长度 &lt;= 7
```
ba=ab
ca=ac
cb=bc
b|=|b
bc=|
a|=
|c=(return)true
=(return)false
```

## 2-8 最多 (c2_8_most)
**输入：** 一个由a, b和c组成的字符串。
**输出：** 返回出现次数最多的字母。
**限制条件：** 1 &lt;= 输入长度 &lt;= 7
保证只有一个字母为最多。
```
ba=ab
ca=ac
cb=bc
aaaa=(return)a
bbbb=(return)b
cccc=(return)c
=abc
```

## 2-9 最少 (c2_9_least)
**输入：** 一个由a, b和c组成的字符串。
**输出：** 返回出现次数最少的字母。
**限制条件：** 2 &lt;= 输入长度 &lt;= 7
保证只有一个字母为最少。
```
ba=ab
ca=ac
cb=bc
abc=(return)b
aab=(return)c
bc=(return)a
=ab
```

## 3-1 去除 (c3_1_Remove)
**输入：** 一个由a, b和c组成的字符串。
**输出：** 移除输入字符串头部和尾部的所有a。
**限制条件：** 1 &lt;= 输入长度 &lt;= 7
```
(start)a=
(end)a=
```

## 3-2 旋转 (c3_2_spin)
**输入：** 一个由a, b和c组成的字符串。
**输出：** 将第一个a字母之前的每个字符移动到序列末尾。
**限制条件：** 1 &lt;= 输入长度 &lt;= 7
输入至少包括一个a。
```
(start)b=(end)b
(start)c=(end)c
```

## 3-3 A到B 2 (c3_3_atob2)
**输入：** 一个由a, b和c组成的字符串。
**输出：** 将输入头部和尾部的每个a替换为b。
**限制条件：** 1 &lt;= 输入长度 &lt;= 7
```
(start)a=(end)|
|=(start)b
(end)a=(start)@
@=(end)b
```

## 3-4 交换 (c3_4_swap)
**输入：** 一个由a, b和c组成的字符串。
**输出：** 交换输入头部的每个a和输入尾部的每个b。
**限制条件：** 2 &lt;= 输入长度 &lt;= 8
输入总是以a开始，以b结束。
```
(end)b=(start)|b
|ba=(end)|ab
|=
```

## 3-5 首尾 (c3_5_match)
**输入：** 一个由a, b和c组成的字符串。
**输出：** 如果输入的首个字母和最后一个字母相同，返回true。
否则，返回false。
**限制条件：** 2 &lt;= 输入长度 &lt;= 7
```
(end)a|=(return)true
(end)b@=(return)true
(end)c$=(return)true
(start)a=(end)|
(start)b=(end)@
(start)c=(end)$
=(return)false
```

## 3-6 最多 2 (c3_6_most2)
**输入：** 一个由a, b和c组成的字符串。
**输出：** 保留出现次数最多的字母并删除其余字母。
**限制条件：** 1 &lt;= 输入长度 &lt;= 7
保证只有一个字母为最多。
```
ab=(end)|c
ba=(end)|c
ca=ac
cb=bc
c|c=(end)|
ac=(end)|
bc=(end)|
a|=aa
b|=bb
|=c
```

## 3-7 回文串 (c3_7_palindrome)
**输入：** 一个由a, b和c组成的字符串。
**输出：** 如果输入是回文串（正向和反向读起来相同），返回true。
否则，返回false。
**限制条件：** 1 &lt;= 输入长度 &lt;= 7
```
a|a@=
b|b@=
c|c@=
(start)a=(end)|a@
(start)b=(end)|b@
(start)c=(end)|c@
@|=(return)false
=(return)true
```

## 4-1 你好 2 (c4_1_hello2)
**输入：** 一个由a, b和c组成的字符串。
**输出：** 在字符串头部添加hello。
**限制条件：** 1 &lt;= 输入长度 &lt;= 7
```
(once)=(start)hello
```

## 4-2 移除 2 (c4_2_remove2)
**输入：** 一个由a, b和c组成的字符串。
**输出：** 移除前三个a。
（如果少于三个a，全部移除）
**限制条件：** 1 &lt;= 输入长度 &lt;= 7
```
(once)a=
(once)a=
(once)a=
```

## 4-3 切割 (c4_3_cut)
**输入：** 一个由a, b和c组成的字符串。
**输出：** 移除前三个字母。
**限制条件：** 3 &lt;= 输入长度 &lt;= 7
```
(once)=(start)|||
|a=
|b=
|c=
```

## 4-4 移除 3 (c4_4_remove3)
**输入：** 一个由a, b和c组成的字符串。
**输出：** 移除字符串最后三个a。
（如果不足三个，全部移除）
**限制条件：** 1 &lt;= 输入长度 &lt;= 7
```
(once)=(end)|||
a|=
b|=|b
c|=|c
|=
```

## 4-5 反转 (c4_5_reverse)
**输入：** 一个由a, b和c组成的字符串。
**输出：** 交换第一个字母和最后一个字母。
**限制条件：** 2 &lt;= 输入长度 &lt;= 7
```
(once)=(start)|
|a=(end)@a
|b=(end)@b
|c=(end)@c
a@=(start)a
b@=(start)b
c@=(start)c
```

## 4-6 反转 2 (c4_6_reverse2)
**输入：** 一个由a, b和c组成的字符串。
**输出：** 反转输入。
**限制条件：** 1 &lt;= Input length &lt;= 7
```
(once)=(start)|||||||
|a=(start)a
|b=(start)b
|c=(start)c
|=
```

## 4-7 切割 2 (c4_7_cut2)
**输入：** 一个由a, b和c组成的字符串。
**输出：** 移除第三个字母。
**限制条件：** 3 &lt;= 输入长度 &lt;= 7
```
(once)=||||||||||||
|b=a
|c=b
||||a=c|||
||=|
|a=
```

## 4-8 复制 (c4_8_clone)
**输入：** 一个由a, b和c组成的字符串。
**输出：** 复制字符串的前三个字母，然后将其添加到字符串的末尾。
**限制条件：** 3 &lt;= 输入长度 &lt;= 7
```
(once)=|||
@a=(start)a
@b=(start)b
@c=(start)c
|a=(end)@aa
|b=(end)@bb
|c=(end)@cc
(once)=@@@
```

## 4-9 A到B 3 (c4_9_atob3)
**输入：** 一个由a, b和c组成的字符串。
**输出：** 将每个a替换为b，将每个b替换为a。
**限制条件：** 1 &lt;= 输入长度 &lt;= 7
```
(once)=(start)|
|a=b|
|b=a|
|c=c|
|=
```

## 4-10 半数 (c4_10_odd2)
**输入：** 一个由a, b和c组成的字符串。
**输出：** 删除每个位于奇数位的字母。（第1,3,5,7个字母）
**限制条件：** 1 &lt;= 输入长度 &lt;= 7
```
(once)=(start)||
||a=|
||b=|
||c=|
|a=a||
|b=b||
|c=c||
|=
```

## 4-11 复制 2 (c4_11_clone2)
**输入：** 一个由a, b和c组成的字符串。
**输出：** 重复一遍输入串。
**限制条件：** 1 &lt;= 输入长度 &lt;= 7
```
(once)=(end)|
@a=(end)a
@b=(end)b
@c=(end)c
a|=|@|aa
b|=|@|bb
c|=|@|cc
|=
```

## 4-12 To B or not to B (c4_12_tob)
**输入：** 一个由a, b和c组成的字符串。
**输出：** 如果输入包括b，将每个a替换为b。
否则，将每个a替换为c。
**限制条件：** 1 &lt;= Input length &lt;= 7
```
(once)b=a|b
b|=|b
c|b=|bc
a|=(end)|b
|b=
a=c
```

## 4-13 中心 (c4_13_center)
**输入：** 一个由a, b和c组成的字符串。
**输出：** 输出最中间的字母。
**限制条件：** 1 &lt;= 输入长度 &lt;= 7
保证字符串长度为奇数。
```
(once)=(start)||||||||||||||||||||||||||||||||||||||||||||||||||||
|a=(end)a
|b=(end)b
|c=(end)c
(start)a=(return)a
(start)b=(return)b
(start)c=(return)c
```

## 4-14 中心 2 (c4_14_center2)
**输入：** 一个由a, b和c组成的字符串。
**输出：** 删除最中间的字母。
**限制条件：** 1 &lt;= 输入长度 &lt;= 7
保证字符串长度为奇数。
```
(once)=@|$||
||a=(end)|@aa
||b=(end)|@bb
||c=(end)|@cc
$|=(start)|
$=$||$||
|@a=
|@b=
|@c=
```

## 4-15 扩张 (c4_15_expansion)
**输入：** 一个由a, b和c组成的字符串。
**输出：** 重复第x个字母x次。
**限制条件：** 1 &lt;= 输入长度 &lt;= 6
```
(once)=||@||@
||@a=(end)|@|a|@|
||@b=(end)|@|b|@|
||@c=(end)|@|c|@|
a|@||@|b|@||@|c=(start)|||@|@
@||@|a=|@||@||@abcaa
@||@|b=|@||@||@abcbb
@||@|c=|@||@||@abccc
|@|=
```

## 4-16 合并 (c4_16_merge)
**输入：** 两个由a和b组成的字符串，以","隔开。
**输出：** 合并两个字符串。
来自两个字符串的字母在输出串当中交替出现。
**限制条件：** 1 &lt;= 输入长度 &lt;= 5
两个输入串长度相同。
```
>,,,a=(end)a
>,,,b=(end)b
,>=(start)>,,,
>,,,,,=
,=,>,,
```

## 5-1 计数 (c5_1_count)
**输入：** 一个二进制数。
**输出：** 该数量的a。
**限制条件：** 1 &lt;= 输入数值 &lt;= 63
```
a1=1aa
a0=1a
1=a
```

## 5-2 A+1 (c5_2_plus)
**输入：** 一个二进制数。
**输出：** 输入加1。
**限制条件：** 1 &lt;= 输入数值 &lt;= 1023
```
(once)=(end)|
1|=|0
0|=1
|=1
```

## 5-3 A+B (c5_3_plus2)
**输入：** 两个二进制数，用+隔开。
**输出：** 二者之和。
**限制条件：** 1 &lt;= 输入数值 &lt;= 31
```
(once)+1=|+
+1=1++
+0=1+
|1=|+
+=@
|=
1@=@0
0@=1
(start)@=(start)1
```

## 5-4 A-B (c5_4_minus)
**输入：** 两个二进制数，用-隔开。
**输出：** 二者之差。
**限制条件：** 1 &lt;= 输入数值 &lt;= 31
第一个数 &gt; 第二个数
```
-1=-|
|1=1||
|0=1|
-|=@-
0@=@1
(start)1@=(start)
1@=0
-=
```

## 5-5 A*B (c5_5_multiply)
**输入：** 两个二进制数，用*隔开。
**输出：** 二者之积。
**限制条件：** 1 &lt;= 输入数值 &lt;= 31
```
@^=^@
%^=^%
&^=+&
1+=+0
0+=1
(start)+=(start)1
@%=%@^
(once)1=(start)0&|@
@1=1@@
@0=1@
|1=|@
|=
(once)*1=$%
%1=1%%
%0=1%
$1=$%
$=
%=
@=
&=
```

## 5-6 A/B (c5_6_div)
**输入：** 两个二进制数，用/隔开。
**输出：** 二者之商和余数，以一个","隔开。
**限制条件：** 1 &lt;= 输入数值 &lt;= 31
```
(once)1=|@
@1=1@@
@0=1@
|1=|@
|=
(once)/1=/$
$1=1$$
$0=1$
/1=/$
(once)=(end)%
(start)/=(start)^
(start)&>=(start)^
>/=/$
/$=&>
>&=&>
@&=*
*>=>*
*$=&>
*%=/%+
>=@
$=
^=
%+=+%
@+=+@
@%=%@
(once)=(start)0-
-+=_-
1_=_0
0_=1
(start)_=(start)1
-=
%=,0<
<@=_<
,_=,1
<=
```

## 6-1 再次问候 (c6_1_hello3)
**输入：** 一个由a, b和c组成的字符串。
**输出：** helloworld
**限制条件：** 1 &lt;= 输入长度 &lt;= 7
在这一关里，你不能使用关键字。
```
b=a
c=a
aa=a
a=helloworld
```

## 6-2 回文串 2 (c6_2_palindrome2)
**输入：** 一个由a, b和c组成的字符串。
**输出：** 如果输入是回文串（正向和反向读起来相同），返回true。
否则，返回false。
**限制条件：** 2 &lt;= 输入长度 &lt;= 7
在这一关里，你不能使用关键字。
```
b=|a|
c=||a||
a@=@a
|@=@|
$a=a$
$|=|$
%a=a%
%|=|%
a$=
|%=
@$=true
@%=true
$=^
%=^
a^=^
|^=^
@^=false
@a=@$
@|=@%
@=true
|=|@
aa=a@a
```

## 6-3 To B or not to B 2 (c6_3_tob2)
**输入：** 一个由a, b和c组成的字符串。
**输出：** 如果输入包括b，将每个a替换为b。
否则，将每个a替换为c。
**限制条件：** 1 &lt;= 输入长度 &lt;= 7
在这一关里，你不能使用关键字。
```
ba=bb
ab=bb
bca=bcb
acb=bcb
bcca=bccb
accb=bccb
bccca=bcccb
acccb=bcccb
bcccca=bccccb
accccb=bccccb
bccccca=bcccccb
acccccb=bcccccb
a=c
```

